# pride-bash
i made a bunch of pride flags in bash. however, i didn't make as much as i could. request more for me to make.

don't request ones with graphics, like the poly flag, because i'm lazy; so i won't work out how to do that.

![cool_image](https://sealo.neocities.org/gren1fsh.gif)

# usage
uhh people are kinda dumb so to use it you just make it a file and use the bash command on linux

to run the file. it'll just print the flag, so if you run `bash transgender.sh`, it will show

the transgender flag.
